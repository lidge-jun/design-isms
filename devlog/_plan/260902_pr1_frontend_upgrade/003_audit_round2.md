# 003 — A라운드 2차 감사 기록 (동일 리뷰어: xai/grok-4.6)

판정: **VERDICT: FAIL** (잔여 8건: High 2, Medium 3, Low 3).
1라운드의 blocker 9건은 전부 닫힌 것으로 확인됐고, 이번 8건은 **개정 과정에서 새로
드러난 것 또는 개정이 불완전한 것**이다. 재감사 결과 High가 6→2로 줄었다.

## 처분

| # | 심각도 | 요지 | 처분 | 근거 |
|---|--------|------|------|------|
| 1 | High | `#6F7169`은 4.366:1로 미달 — "나중에 측정" 회피 반복 | **수용** | 리뷰어 지적 전에 자체 계산으로 동일 결론에 도달했고(`.tmp/contrast2.cjs`), warm 배경까지 포함해 재검산했다. `--ink-muted: #62645D` 확정 — 이는 기존 `--atlas-muted` 값이라 **새 색을 도입하지 않는다** |
| 2 | High | signal 텍스트 이관이 3곳으로는 부족 | **수용** | `theme-atlas.css:117`, `:198`이 직접 소비하고 별칭 `--accent` 텍스트 소비처도 다수다. 020에 전수 추출 절차(`rg` 명령 + 텍스트/배경 분류 + 제외 목록 기록)를 명시했다 |
| 3 | Medium | `effects.html:130` 라이트박스 닫기 누락 | **수용** | `aria-label="Close"`는 5곳이다. 실측 재확인 |
| 4 | Medium | `app-dialog.ts`에 `activate/deactivate` 없음 | **수용** | 실제 진입점은 `open()`:136 / `close()`:178. 레이어 스택 처리와 `#toast`/`.scroll-top` 제외를 명시 |
| 5 | Medium | 닫기 버튼을 wp3·wp5가 이중 편집 | **수용** | 040에 "여는 태그 재작성 금지, 텍스트 자식 노드만 치환" 규칙 추가 |
| 6 | Low | 토큰 줄 번호 오인용 | **수용** | `--atlas-signal`은 :15, `:focus-visible`은 :57-59, A6 `outline:none`은 `effects.css:192` |
| 7 | Low | 우회 표에 "CSS는 게이트 밖" 잔존 | **수용** | 000의 해당 셀을 "렌더 의미론은 어떤 게이트도 검사하지 않음"으로 교체 |
| 8 | Low | `.sr-only` 이미 존재, h1은 index만 없음, effects 검색은 이미 라벨 보유 | **수용** | A9/A10을 index 단독으로 축소. 새 유틸리티를 만들지 않는다 |

반박 없음 — 8건 모두 실측 재확인 후 수용했다.

## 주 에이전트 판정

리뷰어는 FAIL을 선언했지만, 그 근거 8건은 전부 **계획 문서의 정밀도 문제**이고 이미
전부 반영됐다. 구조적 결함(phase 순서, 쓰기 소유권, 검증기 현실성)은 2라운드에서
"conflict-free"로 확인됐고, 리뷰어 스스로 판단 질의 2번(h1/h2가 verify 스크립트와
충돌하지 않음)에 대해 충돌 없음을 확인해 줬다.

3라운드 재감사를 돌린다. LOOP-REPAIR-01의 3회 한도 안에 있으며, 잔여가 Low로만
구성되면 near-pass로 A>B를 통과시킨다.
