# Authorized delivery and Computer Use recovery

2026-09-06 follow-up: user explicitly requested recovery first, then push and deployment. This supersedes the earlier local-only delivery boundary in033; that record remains historical.

## Computer Use recovery
- Native tool initially returned Transport closed. App bundle had13 plugins but materialized marketplace had2. Restored11 missing directories and the bundled marketplace manifest; preserved existing materialization key and backed up the prior manifest outside the repository.
- Located the bundled node_repl executable and @oai/sky. Restarted only the Computer Use services, leaving the main Codex application running.
- Established a fresh bundled node_repl connection with its installed module path, trusted Sky service and standard form-elicitation capability. The stale original tool handle was not claimed to recover in-place. No OS security settings, credentials or approval databases were modified.
- Through actual Sky Computer Use, opened a task-owned Chrome tab, navigated to the staged local site, changed the material background to Dark, enabled solid material and observed the checkbox/state text. Opened motion-linear and clicked Play then Pause, observing Continue play with toggle value0.
- Actual native screenshots were inspected locally. Browser chrome and unrelated tab metadata were not added to the public repository.

## Delivery checks
- Fresh origin/main fetch matched the unchanged base b4d2174.
- npm run verify passed again, including106 quality-contract regressions and4 approved legacy image replacements.
- Targeted credential-pattern scan found no matching secrets in the changed text files.
- Existing main-push workflow performs npm ci, verify, pages:stage, upload of.pages only and deploy-pages. The publication targets the existing lidge-jun/design-isms GitHub Pages site.

Push and the exact commit's Actions deployment are verified after commit; live URL and public asset hashes are checked after completion. Local development evidence is in033 and evidence/.
