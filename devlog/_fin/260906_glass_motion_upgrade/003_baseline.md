# Baseline inspection

2026-09-06. Fresh upstream: b4d2174, HEAD...origin/main = 0 0, initially clean.
Full npm run verify passes; stored evidence/baseline-verify.log.
Opened actual local pages from npm run serve -- --root . --port 4180.
Inspected glass modal screenshot screenshot_1788677285887.png and motion grid screenshot_1788677399651.png under ~/.browser-agent/screenshots.
Motion grid contains largely isolated shapes; visible text overlaps schematic lines in a fade specimen. Current behavior needs richer, readable scenes without altering catalog identity.

## Tool capability observations
Aside 1.26.902.1732 is available. ima2 ping succeeds at localhost:3333, v3.10.0, zero active jobs at probe.
Current agbrowse executable supports navigate/resize/screenshot; `script` is absent and returns help with exit 0. Treat that as unsupported, not successful QA. Use supported commands or the repository's existing CDP test approach. No dependencies installed.
Computer Use plugin exposes native methods but requested skill requires node_repl/@oai/sky, which is absent in current tools. No substitute native interaction claimed.

## Audit coupling
scripts/verify-image-quality.mjs compares complete immutable legacy runtime payloads after replacing approved prompt fields. Editorial changes therefore need a narrow, explicit revision receipt; deleting that comparison would silently remove unrelated-data protection. Immutable 093-097 files must remain unchanged.

Image reviewer correction: the glass landing product photography flows into the background, so it is not accurately described as a separate boxed product image. Selection rationale is oversized serif hierarchy and redundant outlined controls. Spring/back raster candidates also contain numerical inaccuracies; compare to CSS mathematical extrema before regeneration.

ima2 localhost:3334 is unreachable; default localhost:3333 responds. The historical attempt runner needs an explicit loopback server override (wp3), preserving historical argv rather than editing old attempts.
