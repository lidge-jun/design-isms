Create a meticulous, finished 1536x1024 landscape implementation guide for a frontend motion catalog. The subject is a subtle spring-like button scale response, specifically scale 0.92 to scale 1 over 500 milliseconds using cubic-bezier(0.34, 1.56, 0.64, 1). This is an educational diagram with real UI components, not a promotional poster. Its purpose is to prevent a common mathematical mistake: the y control point 1.56 is NOT a physical scale of 1.56. The easing progress reaches about 1.0978 and the actual interpolated button scale reaches about 1.0078, then settles at 1. Use these exact values, with the rounded displayed peak '1.008'. Never draw a button becoming 56% larger. The peak should look almost the same size as the settled state.

Art direction: quiet, precise editorial technical plate. Off-white near-neutral background, graphite text, one restrained burnt-orange accent for active state, no purple, no pastel rainbow, no gradients, no inflated 3D icons, no glass panels in this particular guide. Clean sans-serif text with a compact monospaced numerical layer. Generous but disciplined margins, aligned left edges, clear section hierarchy, sharp vector-like strokes, consistent 1px rules. The top-left heading is 'Spring response'. Under it a single concise sentence: 'A small overshoot, then a stable resting state.' Upper-right metadata: '500 ms'. Typography must remain readable when the image is reduced to 768x512; omit small extra prose rather than fitting illegible text.

The main content is a left-to-right three-state sequence occupying the middle two-thirds. Each state shows the SAME flat dark rectangular rounded button labelled 'Save', with a faint outline showing the final size as a reference. State one: 'Pressed' and 'scale 0.92'. State two: 'Overshoot' and 'scale 1.008'. State three: 'Settled' and 'scale 1.00'. Use simple thin horizontal connecting arrows. Draw the first button approximately 8 percent smaller, the next two practically equal. Do not exaggerate the second frame for dramatic effect. The first frame is a starting state, not a trigger instruction.

Below this sequence, use one full-width technical note with precisely typeset code: 'cubic-bezier(0.34, 1.56, 0.64, 1)'. Immediately below: 'Control points shape progress. They are not scale values.' A second small note reads 'Scale = 0.92 + 0.08 × progress'. Do not draw an easing graph or a table of sampled values, because there is no need for invented timing samples. No plotting labels, axes, chart percentages, or unrelated spring physics constants.

The bottom strip is an accessible alternative with a simple outline switch icon and the heading 'Reduced motion'. Show a single normal-size Save button and the sentence 'Show scale 1 immediately. Keep the action and focus.' Use a subtle flat pale-green tint only if needed to distinguish this strip; prefer a horizontal rule instead. No checkmark badges claiming certification. Nothing is a fabricated brand logo. The result must be mathematically accurate, typographically polished, visually restrained and useful as an implementation reference. Include no watermark, browser chrome, device mockup, photos, stock characters, or text beyond the specified labels. Output one complete high-resolution guide image.
