# wp3 build and image review ledger

## Generation scope
Four prompts prepared in evidence/prompts. Motion candidates spring-1/back-1 started through ima2 direct mode, oauth/gpt-5.6-sol/high,1536x1024,300s at existing localhost3333. No global defaults or account changes. Candidate target paths predetermined in evidence/motion-generation-manifest.json. Total candidate budget12; two requested so far.

## Numerical guidance
Updated spring/back summary to distinguish Bézier control points from actual property values. No easing/duration/snippet changes. 005 analytic oracle: scale peak1.007824; Back displacement extrema-2.513836/26.513836px, target24px.

## Pipeline
Kant owns local successor/profiles code. Historical3334 attempts remain exact; new profile makes current3333 explicit. Previous098 SHA and 662 raster/15 immutable baseline hashes captured in pre-image-hashes.json.

## Candidate review round1
spring-1: REJECTED. Numerical labels corrected, but generated smoky dark/translucent material destroys title/body contrast despite original white/no-glass brief. Regeneration hypothesis: explicitly lock opaque white textbook surface and forbid all material/lighting effects at the beginning and end of prompt. spring-2 prompt records that correction. back-1: candidate retained for review; labels correctly distinguish control points and displacement, old false graph removed. Main inspected both actual PNGs. Three total candidates requested after spring retry.

## Candidate review correction
Independent image reviewer Cicero REJECTED back-1: numeric labels correct but Anticipate button centered at0 and Overshoot centered at24, contradicting their meanings. Accepted; back-2 uses targeted ima2 edit to move second button20 image pixels left and third18 right while keeping fixed position markers and all correct text. This matches the approximately7.3× visual displacement scale. No manual image manipulation. Candidate count4 including edit.

ima2 edit CLI differs from gen: it rejects lane/model form before submission. First edit invocation exited2 without a job; retried with explicit --provider oauth --model gpt-5.6-sol, preserving rejected CLI log and effective argv. This is command-shape recovery, not another generated candidate.

back-2: REJECTED by main and independent reviewer; targeted edit barely changed geometry. back-3 changes representation: deliberately exaggerated schematic, explicitly not to scale, fixed zero/target markers with whole button visibly left/beyond target. Actual numerical captions stay precise. This addresses the failed mechanism (tiny pixel edit not expressed) rather than repeating it. Candidate count5.

## Motion acceptance and application
Main and Cicero accepted spring-2 and back-3. Source and preview hashes, successful request IDs and exact prompts are in evidence/motion-selected.json. Two PNGs applied; images:thumbs generated2 and retained18 fresh motion previews (331 total manifest pairs). Exactly two current061 ledger/audit rows updated; former rows preserved in evidence/motion-guide-ledger-before.json. Captions state diagrams are schematic and numeric/CSS values are authoritative. verify:catalog passed.

## Pipeline handoff
Kant completed allowed seven scripts and106 contract tests. Main independently ran the same106 tests successfully. Old rejected unprofiled experiments retain historical evidence; newprofile/run/accepted records use strict command validation. Legacy img-000007/000008 generation started only after implementation and tests completed.

## Pipeline independent audit
Darwin: PASS, no blockers; reran106 contract tests. Confirmed exact archive reuse, expected-old before no-op, atomic receipt rename, chain/approved-cell continuity, strict profile paths and retained editorial/full-runtime/current-sheet verification. Actual finalization remains to be executed by main.

## Glass candidate selection
img-000007 landing: ACCEPT main+Cicero, coherent product stage with one glass nav, no redundant controls. img-000008 mobile: REJECT main+Cicero due white header labels over pale sky, despite successful single-playback hierarchy. img-000009 retries with explicit dark graphite top labels and dark-circle/white-icon separation. Total8 generated candidates requested including rejected variants; within12 bound.

Header diagnostic: raw PNG pixel at(300,46), adjacent to Now playing, is RGB156/172/182, giving only about2.34:1 against white. This corroborates the visual rejection; it is a local background sample, not whole-image accessibility certification. No raster pixels were manually modified.

## Image runtime cache correction
Staged browser inspection showed new image URLs still used the old2026-07-17 index cache token and unversioned motion guide URLs. Updated src/app.ts image version and src/motion.ts guide preview/source versions so previously cached reference images refresh with the upgrade. This is scoped integration required by actual image replacement. The initial lightbox probe also used endsWith('.png') on a URL with a query; corrected oracle uses URL.pathname and asserts the result, rather than treating the earlier false field as pass.

## Finalization and preservation
Accepted legacy img-000007 and img-000009 applied with exact prepared prompts and matching WebP hashes; img-000008 remains a rejected attempt. Pre-final passed211 audited/4 approved replacements. Successor finalization completed with old receipt1391e423b6cad0bb7f032d7b6832a169a9cf2cfbe8684640927fa863c51c4dc2 preserved and new receiptdeac17b8bc081e1328fb4a4a5b027dfac7036cf1efa28515896693c8d808b1c7. Actual current 4-sheet/211-cell aggregate cf4e9de33b6ab6e13ab2c24c7b45182f07459152c1061d576b561de882f1b844. Main inspected changed final contact sheets.

image-preservation.json proves exactly8 raster paths changed (4PNG+4WebP),654 remain identical, all15 recorded immutable baseline/old-sheet paths identical, and archived previous098 bytes match its SHA. images:audit passed94 effect pairs, no invalid/orphan files. Build and pages:stage passed7HTML/331PNG/331WebP/0forbidden.

## Staged browser evidence
final-pages.json: all7 pages at1440/390, expected counts49/94/18/25/20/25/20, no horizontal overflow, broken loaded images, console or runtime errors. final-image-runtime.json: four updated previews plus tested PNG lightboxes use fresh2026-09-06 query versions, correct dimensions and actual served bytes equal the manifest. Main inspected the staged Liquid Glass and Back guide screenshots. Source shape and browser contexts are Chromium/CDP; native Computer Use node_repl was unavailable, and no native-OS motion or Safari/Firefox certification is claimed.
